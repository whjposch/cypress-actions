name: SW-Admin Testing
on:
  pull_request:
  merge_group: # TODO: Every commit to an "open" pr & creation of PR
  workflow_dispatch:
    inputs:
      testType:
        description: 'The type of test'
        required: true
        default: 'full'
jobs:
  block-if-not-tested:
    name: Block it not tested
    runs-on: ubuntu-latest
    steps:
      # Step 1: Check if latest commit is tested
      # Step 2.1: Block PR if not tested
      # 2.2: Unblock PR if
      - name: Check if tested
        run: echo "Heeeey ${{github.actor }}"
      - name: Fail
        run: exit 1
